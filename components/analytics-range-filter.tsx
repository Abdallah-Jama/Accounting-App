import type { AnalyticsFilters } from "@/lib/analytics";
import { primaryButton, secondaryButton } from "@/components/ui";

export function AnalyticsRangeFilter({filters}:{filters:AnalyticsFilters}){return <form method="get" className="flex flex-col gap-3 rounded-2xl border bg-white p-4 shadow-soft lg:flex-row lg:items-end"><div className="min-w-48"><label htmlFor="range">Reporting period</label><select id="range" name="range" defaultValue={filters.preset.toLowerCase().replace(/_/g,"-")}><option value="this-month">This month</option><option value="last-month">Last month</option><option value="this-year">This year</option><option value="custom">Custom range</option></select></div><div><label htmlFor="from">From</label><input id="from" name="from" type="date" defaultValue={filters.from}/></div><div><label htmlFor="to">To</label><input id="to" name="to" type="date" defaultValue={filters.to}/></div><div className="flex gap-2"><a href="?range=this-month" className={secondaryButton}>Reset</a><button className={primaryButton}>Apply range</button></div><p className="text-xs leading-5 text-slate-500 lg:ml-auto lg:max-w-xs">Period flows use these dates. Money in hand and company balance counts are calculated as of the end date.</p></form>}
