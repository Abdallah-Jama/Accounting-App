"use client";
import { changePasswordAction, type AuthFormState } from "@/app/auth-actions";
import { primaryButton } from "@/components/ui";
import { useActionState } from "react";
export function ChangePasswordForm(){const [state,action,pending]=useActionState<AuthFormState,FormData>(changePasswordAction,{});return <form action={action} className="mt-5 grid max-w-2xl gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><label htmlFor="currentPassword">Current password</label><input id="currentPassword" name="currentPassword" type="password" autoComplete="current-password" required/></div><div><label htmlFor="newPassword">New password</label><input id="newPassword" name="password" type="password" autoComplete="new-password" minLength={10} required/></div><div><label htmlFor="newConfirmation">Confirm new password</label><input id="newConfirmation" name="confirmation" type="password" autoComplete="new-password" minLength={10} required/></div>{state.error&&<div role="alert" className="sm:col-span-2 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{state.error}</div>}<div className="sm:col-span-2"><button className={primaryButton} disabled={pending}>{pending?"Changing…":"Change password"}</button></div></form>}
